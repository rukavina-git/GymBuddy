<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only: lets the debug build reach a local backend over plain
  HTTP without weakening release, which has no network security config
  and no cleartext permission at all (see the main manifest's
  usesCleartextTraffic="false").
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator's alias for the host machine's loopback. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
