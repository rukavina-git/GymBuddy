# compileSdk (37) is ahead of what Robolectric 4.15.1 ships shadows for.
# None of the tests that use Robolectric need real device behavior beyond
# a Context and a working SQLite (Room in-memory DB, DataStore) - pin to
# a long-stable, definitely-supported level rather than the app's own
# compileSdk.
sdk=34
