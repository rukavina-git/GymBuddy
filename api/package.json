{
  "name": "gymbuddy-api",
  "private": true,
  "scripts": {
    "lint": "spectral lint openapi.yaml",
    "docs": "redocly build-docs openapi.yaml -o docs.html",
    "generate:kotlin": "openapi-generator-cli generate -i openapi.yaml -g kotlin --additional-properties=library=jvm-retrofit2,serializationLibrary=moshi -o ../android/generated-client"
  },
  "devDependencies": {
    "@stoplight/spectral-cli": "^6.11.0",
    "@redocly/cli": "^2.0.0",
    "@openapitools/openapi-generator-cli": "^2.15.0"
  }
}