name: API Docs

on:
  push:
    branches: [ main ]
    paths:
      - 'api/**'
      - '.github/workflows/api-docs.yml'

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Lint and Build Swagger UI
    runs-on: ubuntu-latest
    permissions:
      contents: read

    defaults:
      run:
        working-directory: api

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: api/package-lock.json

      - name: Install dependencies
        # --ignore-scripts: package lifecycle scripts (pre/postinstall)
        # don't get to run arbitrary code during install.
        run: npm ci --ignore-scripts

      - name: Lint spec with Spectral
        run: npm run lint

      - name: Build Swagger UI site
        run: npm run build:docs-site

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: api/site

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
